#include <iostream>

using namespace std;

int main()
{
    //caller(name, problem, phoneNumber)
    //create queue of callers

    //phoneLine(employeeName, employeeID)
    //create queue of phoneLines

    //while callers

        //when caller is processed they go to the first employee

        //employee receives call and moves to array of busy employees

        //check array of busy employees and move nonbusy employees to queue



    return 0;
}
